# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
venv/
ENV/
env/
.env
.venv
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environment configuration
.env
.env*.local

# Encryption keys and keyfiles (keep env.enc if it is a template/shared asset, but exclude plain text secrets)
# The user's repo has .env.enc which contains encrypted api keys. Let's keep it tracked since it was in the workspace.

# OS files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
